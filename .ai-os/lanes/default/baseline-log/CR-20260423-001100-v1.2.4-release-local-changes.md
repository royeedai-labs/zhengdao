# 变更请求：v1.2.4 本地未提交改动发布收口

- **变更标题**：发布 `v1.2.4`，纳入当前本地未提交的产品体验、AI-OS v9 和稳定性改动
- **提出时间**：2026-04-23 00:11:00 Asia/Shanghai
- **变更原因**：用户明确要求发新版 Release，并指出本地还有很多代码未提交，需要把发布环节也补上。
- **优先级**：P0
- **治理档位**：high-risk（Git commit / tag / push / GitHub Release 属于外部副作用）

## 目标

- 将当前本地已验证的未提交改动整理为 `v1.2.4` patch release。
- 补齐版本号、`CHANGELOG.md`、release plan、risk register、verification matrix 和状态记录。
- 提交代码、创建 `v1.2.4` tag 并推送远端，让 GitHub Actions 生成安装包和自动更新元数据。
- GitHub Release 正文必须包含更新日志、安装包与自动更新清单、发布注意事项、验证状态和回滚提示。

## 发布范围

| 变更组 | 范围 | 说明 |
|---|---|---|
| AI-OS v9 | `AGENTS.md`、`.ai-os/`、`CLAUDE.md`、`GEMINI.md`、旧 `.agents/` / `.cursor/` 删除 | 将项目交付治理切到 v9 canonical layout。 |
| 工作区与主题 | `TopBar`、`WorkspaceLayout`、`ui-store`、`themes`、`index.css`、workspace layout utils | 默认跟随系统主题、冷白浅色、低饱和深色、侧栏宽度和工具折叠持久化。 |
| 核心弹窗视觉 | bookshelf、bottom panel、modals、sidebar、shared components | 统一语义色和低饱和视觉表达。 |
| AI 助手交互 | `AiAssistantDock`、`panel-layout`、相关 tests | 悬浮入口可拖动，展开态不再有独立浮动 X，面板 / 入口尺寸位置 clamp。 |
| 关系图谱修复 | `RelationGraph` | 修复 ResizeObserver / canvas 高度反馈导致名称持续下移。 |
| 发布验证 | `CHANGELOG.md`、package version、release artifacts | 版本、tag、Release 正文和自动更新元数据保持一致。 |

## 非目标

- 不在本次手工生成或上传安装包；安装包由 tag 触发的 GitHub Actions release workflow 产出。
- 不引入数据库 schema 迁移。
- 不处理签名、公证或 Windows 实机人工验收。
- 不清理旧失败 release / tag。

## 风险与审批

- **外部副作用**：commit、tag、push 和 GitHub Release 更新不可当成本地试运行处理。
- **审批结论**：用户已明确要求“要发新版 release”，本 CR 只在验证通过后执行 `v1.2.4` 发布。
- **回滚条件**：若 GitHub Actions 打包失败、Release 缺失安装资产 / 更新元数据 / 正文，或自动更新元数据异常，则不得宣布发布完成；必要时发布 `v1.2.5` 修复。

## 验证要求

- `npm test`
- `npm run build`
- `git diff --check`
- GitHub Release 远端复核：非 draft、非 prerelease、正文包含必要内容、资产包含 Windows installer、macOS dmg / zip、`latest.yml`、`latest-mac.yml` 和 `.blockmap`
