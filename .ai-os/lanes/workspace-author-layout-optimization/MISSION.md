# 全应用冷白主题与作者工作区布局优化 Mission

## 1. 交付基线摘要

- **宿主项目 / 系统**：证道 Electron 桌面端创作软件
- **当前交付主题**：全应用冷白主题与作者工作区布局优化
- **当前交付目标**：把默认外观改为跟随系统的浅/深模式，浅色采用冷白效率方向，并把工作区布局收口为作者优先写作、资料与辅助能力按需出现。
- **成功标准**：
  - 默认主题为 `system`，首次启动跟随系统浅/深外观。
  - 浅色主题以冷白、蓝灰、石墨文字为主，主操作为低饱和蓝，状态色语义清晰。
  - 深色主题同步降饱和，不再以高亮 emerald / indigo / purple 作为主视觉。
  - 工作区顶栏、左右栏、编辑区、AI 助手、底部沙盘和核心弹窗接入统一主题变量。
  - 左右栏宽度、右侧上下文 tab、顶栏折叠偏好本地持久化，不改变业务数据。
- **项目模式**：change
- **当前治理档位**：P1
- **当前基线 ID**：CR-20260422-author-layout-cold-white-theme

## 2. 范围边界

### 范围内
- 主题 token、默认主题模式和系统外观跟随
- 工作区顶栏分组与窄窗口工具折叠
- 左右侧栏宽度持久化与右侧上下文 tab
- 编辑区浅色冷白体验
- 书架、工作区、核心弹窗、AI 助手、底部沙盘、命令面板和确认弹窗的主题变量接入

### 范围外
- SQLite schema、IPC、preload API、业务数据结构
- AI provider、账号授权、发布流程和打包配置
- 重做完整信息架构或删除现有功能入口

## 3. 已确认约束与关键决策

- 默认主题采用 `system`，不按应用内部时间硬切换。
- 主视觉采用冷白效率方向；文学感通过留白、字体层级和少量金色点缀体现。
- 全应用视觉统一允许覆盖，但不得改变业务逻辑和数据流。
- 保留现有 `leftPanelOpen/rightPanelOpen`、ModalType、Zustand 业务 store 和入口调用方式。

## 4. 验收入口

| 需求 ID | 需求点 | 验收入口 |
|---|---|---|
| REQ-LAYOUT-001 | 默认跟随系统外观并支持手动覆盖 | AC-LAYOUT-001 |
| REQ-LAYOUT-002 | 全应用冷白效率浅色与低饱和深色 | AC-LAYOUT-002 |
| REQ-LAYOUT-003 | 作者工作区布局优先写作且辅助按需出现 | AC-LAYOUT-003 |
| REQ-LAYOUT-004 | 不改变业务接口、数据流和持久化 schema | AC-LAYOUT-004 |
