# 应用更新体验重构 Mission

## 交付基线摘要

- **宿主项目**：证道 Electron 桌面端创作软件
- **项目模式**：change
- **治理档位**：P1
- **风险档位**：medium
- **当前基线 ID**：CR-20260423-013500-update-experience-redesign
- **交付目标**：把现有“后台自动下载 + 标题栏更新按钮”重构为“后台自动检查 + 发现即弹出全局更新页 + 用户手动下载/安装 + 安装失败可回退重试”的完整更新闭环。

> 2026-04-23 追加当前修复：`CR-20260423-165757-update-reopen-html-macos-guard` 修复启动 / 重新打开应用时未主动检查更新、更新日志展示 HTML、macOS 未签名公开测试包触发 ShipIt 签名校验失败。

## 已确认范围

- 在线更新继续基于 GitHub Releases 和 `electron-updater`。
- 应用仍在打包版后台自动检查更新，但不再自动下载。
- 发现新版本后，立即弹出新的全局“应用设置 / 关于”模态页，展示版本号、发布日期和更新日志。
- 用户点击“下载更新”后才开始下载；下载完成后仍在同一入口里执行“立即安装”。
- 关闭弹框后，书架页和工作区都必须能重新打开“应用设置 / 关于”查看更新状态。
- 若 `quitAndInstall()` 未让应用在 15 秒内退出，UI 必须从“正在安装”回退成可重试状态，并给出明确错误提示。
- 打包版启动、重新打开窗口和重新激活应用时必须触发去抖更新检查，避免长期未退出用户错过新版本提示。
- 更新日志必须展示可读纯文本，不得直接展示 HTML 标签。
- 当前 macOS 未签名公开测试包发现新版本后降级为手动下载，不触发应用内下载 / ShipIt 自动安装。

## 非目标

- 不改动 GitHub Release 生产流程、签名、公证或更新元数据格式。
- 不新增“跳过此版本 / 永久忽略”能力。
- 不把作品级“项目设置”混入应用级更新能力。

## 验收映射

| 需求 ID | 需求点 | 验收入口 |
| --- | --- | --- |
| REQ-UPD-001 | 发现新版本立即弹出全局更新页 | AC-UPD-001 |
| REQ-UPD-002 | 用户手动下载并查看下载进度 | AC-UPD-002 |
| REQ-UPD-003 | 下载完成后从更新页安装并支持失败回退 | AC-UPD-003 |
| REQ-UPD-004 | 书架页与工作区都能重新打开“应用设置 / 关于” | AC-UPD-004 |
| REQ-UPD-005 | 更新状态与帮助文档口径一致 | AC-UPD-005 |
| REQ-UPD-006 | 启动 / 重开 / 激活应用会主动检查更新 | AC-UPD-006 |
| REQ-UPD-007 | HTML 更新日志清洗为可读文本 | AC-UPD-007 |
| REQ-UPD-008 | 未签名 macOS 包不触发 ShipIt 自动安装 | AC-UPD-008 |
